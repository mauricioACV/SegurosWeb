@model IEnumerable<Seguros.Web.FrontEnd.Models.Requerimiento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_req)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_limit_req)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_fin_real)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_req)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_limit_req)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_fin_real)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
